databases:
  - name: smartops-db
    databaseName: smartops
    plan: free

services:
  - type: web
    name: smartops-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npx prisma generate
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: smartops-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true 
      - key: OPENAI_API_KEY
        sync: false 

  - type: static          # <--- CHANGED: Was 'web', now 'static'
    name: smartops-frontend
    rootDir: frontend
    buildCommand: npm install && npm run build
    publishDir: dist      # <--- Now valid because type is 'static'
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: smartops-backend
          property: host